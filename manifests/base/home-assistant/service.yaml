apiVersion: v1
kind: Service

metadata:
  name: home-assistant-service
  labels:
    app: home-assistant

spec:
  type: ClusterIP
  selector:
    app: home-assistant

  ports:
    - protocol: TCP
      port: 8123
      targetPort: 8123