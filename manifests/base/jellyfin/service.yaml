apiVersion: v1
kind: Service

metadata:
  name: jellyfin-service
  labels:
    app: jellyfin

spec:
  type: ClusterIP
  selector:
    app: jellyfin

  ports:
    - protocol: TCP
      port: 8096
      targetPort: 8096
      name: http